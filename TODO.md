# TODO

本文件记录项目级、可执行的开发顺序。Magnet / BitTorrent 的完整产品设想与原始清单见
[`docs/Magnet-BitTorrent-Download-Feature.md`](docs/Magnet-BitTorrent-Download-Feature.md)。

## 当前优先级

### M0：实施准备

- [ ] 收缩首版范围，只保留 `.torrent`、Magnet、文件选择、下载控制、必要状态和恢复能力
- [ ] 定义统一的下载协议与任务边界，确保现有 HTTP 下载行为不回归
- [ ] 设计可演进的任务持久化结构；在迁移到 SQLite 前明确现有 JSON 队列的兼容方案
- [ ] 通过适配层接入稳定版 MonoTorrent，并固定依赖版本
- [ ] 明确 BitTorrent 引擎的进程级生命周期、取消、异常隔离和退出顺序
- [ ] 增加第三方依赖许可清单，保留 MonoTorrent 的 MIT 许可声明
- [ ] 完成安全设计：阻止绝对路径、`..` 越界、Windows 保留名、重名覆盖及非法文件名
- [ ] 完成隐私提示：说明 DHT、Tracker、PEX、对等上传和公网 IP 暴露，并默认关闭完成后做种
- [ ] 建立本地、确定性的 BitTorrent 测试网络，不以公共 Torrent 作为 CI 的必要条件

完成门槛：架构方案可由当前代码逐步迁移，测试工程可运行，现有 HTTP 下载测试全部通过。

### M1：`.torrent` 最小闭环

- [ ] 导入并校验 `.torrent` 文件
- [ ] 展示经过路径安全校验的文件树、总大小和可选文件
- [ ] 创建 Torrent 任务并接入现有任务列表
- [ ] 支持开始、暂停、继续、停止和移除
- [ ] 展示进度、下载/上传速度、Peer/Seed 数量和 ETA
- [ ] 区分等待、无 Peer、停滞、校验中、完成和失败状态
- [ ] 下载前检查磁盘空间，且不静默覆盖已有文件

完成门槛：在本地测试网络中可完成单文件和多文件 Torrent 下载，暂停后可在本次运行内继续。

### M2：Magnet 最小闭环

- [ ] 解析和规范化 Magnet URI，拒绝无有效 InfoHash 的输入
- [ ] 支持获取元数据、取消获取以及超时/无来源状态
- [ ] 将元数据获取状态与文件下载状态分开建模
- [ ] 获取元数据后展示文件选择，再开始正式下载
- [ ] 展示 Tracker、DHT 和连接状态摘要
- [ ] 识别重复的 InfoHash，避免重复创建任务

完成门槛：在本地测试网络中可从 Magnet 获取元数据、选择文件并完成下载。

### M3：持久化与恢复

- [ ] 持久化协议、来源、InfoHash、保存目录、文件选择和任务状态
- [ ] 保存并恢复 MonoTorrent Fast Resume 数据
- [ ] 实现正常退出保存、异常退出恢复和不完整数据降级处理
- [ ] 恢复前校验目标文件、剩余磁盘空间和任务版本
- [ ] 覆盖重复任务、文件已存在、文件被移动及恢复数据损坏场景

完成门槛：关闭或异常终止应用后，重启可恢复任务且不会破坏已下载数据。

### M4：设置与体验完善

- [ ] 增加全局下载和上传限速
- [ ] 增加 DHT 开关和完成后的做种策略，默认完成即停止上传
- [ ] 完善 Torrent 任务详情、错误信息和可操作的恢复提示
- [ ] 将 Torrent 关键生命周期、Tracker/DHT 状态和恢复结果写入脱敏日志
- [ ] 补齐无网络、无 Peer、Tracker 失败、磁盘写入失败和 Hash 失败测试
- [ ] 发布前完成大文件、长时间运行、退出和崩溃恢复测试

完成门槛：核心状态可诊断，设置可持久化，发布验证不依赖公网资源。

## 暂缓项

- [ ] 文件优先级的高级控制
- [ ] Peer 列表、Tracker 明细和 Piece 视图
- [ ] Ratio / 按时间做种及每任务限速
- [ ] UPnP、NAT-PMP 和端口状态检测
- [ ] BitTorrent v2 / Hybrid 的高级诊断界面
- [ ] 顺序下载、流式播放、远程 API、浏览器扩展和 CLI
